name: compute-requester
description: |
  A short-lived sub-agent instantiated with its own freshly created ATProto
  account. Entry tool enrolls via Welcome Mat, writes com.fedproxy.rbac
  (role=root, full CRUD on *), then writes com.publicdomainrelay.temp.compute.vm
  and com.publicdomainrelay.temp.market.rfp on its own account.
# spawnsSubAgent: true
# bootstrap: welcome-mat-on-enroll
# entryTool: run_compute_requester_subagent
skills:
  - createNewATProtoAccount
  - computeContractCreate
