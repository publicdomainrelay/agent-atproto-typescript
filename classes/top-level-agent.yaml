name: top-level-agent
description: |
  Long-running agent. Handles webhook events and spawns compute-requester
  sub-agents when VMs are needed, keeping each compute job isolated under
  its own DID and RBAC policy.
spawnsSubAgent: false
bootstrap: atproto-password
skills:
  - replyOnBsky
  - spawnComputeRequester
